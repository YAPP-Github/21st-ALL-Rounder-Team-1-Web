'use client';
import { StyledLayout } from 'components/shared';

const Registration = () => {
	// const [count, setCount] = useAtom(countAtom);

	return <StyledLayout.BoxFlexCenter></StyledLayout.BoxFlexCenter>;
};

export default Registration;
