'use client';
import { Tab } from 'components/feature';

const Registration = () => {
	// const [count, setCount] = useAtom(countAtom);

	return (
		<>
			<Tab />
		</>
	);
};

export default Registration;
