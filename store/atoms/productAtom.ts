// import { atom } from 'jotai';
// import { atomFamily } from 'jotai/utils';

// const productAtom = atom([{ brandName: '', productName: '', isProductEmptyError: 'normal' }]);
// const productArrFamily = atomFamily((category: string) => atom((get) => get(productAtom)[category]));
